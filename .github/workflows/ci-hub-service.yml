name: CI - Hub Service

on:
  push:
    branches: [main, develop]
    paths:
      - "apps/hub-service/**"
      - "libs/common/**"
      - "config-repo/hub-service.yml"
      - "config-repo/application*.yml"
      - "build.gradle"
      - "settings.gradle"
      - "gradlew"
      - "gradlew.bat"
      - "gradle/**"
      - ".github/workflows/ci-module.yml"
      - ".github/workflows/ci-hub-service.yml"
  pull_request:
    branches: [main, develop]
    paths:
      - "apps/hub-service/**"
      - "libs/common/**"
      - "config-repo/hub-service.yml"
      - "config-repo/application*.yml"
      - "build.gradle"
      - "settings.gradle"
      - "gradlew"
      - "gradlew.bat"
      - "gradle/**"
      - ".github/workflows/ci-module.yml"
      - ".github/workflows/ci-hub-service.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-hub-service-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  test:
    uses: ./.github/workflows/ci-module.yml
    with:
      module-name: hub-service
      gradle-task: ":apps:hub-service:build"
      test-report-path: apps/hub-service/build/reports/tests/
      use-postgres: true
