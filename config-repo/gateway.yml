server:
  port: ${GATEWAY_SERVER_PORT:8080}

spring:
  cloud:
    gateway:
      server:
        webflux:
          routes:
            - id: hub-service
              uri: lb://hub-service
              predicates:
                - Path=/api/v1/hubs,/api/v1/hubs/**
