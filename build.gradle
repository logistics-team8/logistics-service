plugins {
    id 'java'
}

ext {
    springdocVersion = '3.0.3'
}

allprojects {
    group = 'com.logistics'
    version = '0.0.1-SNAPSHOT'

    repositories {
        mavenCentral()
    }
}

subprojects {
    apply plugin: 'java'

    java {
        toolchain {
            languageVersion = JavaLanguageVersion.of(21)
        }
    }
}
