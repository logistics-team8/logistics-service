spring:
  application:
    name: hub-service
  profiles:
    default: local

---

spring:
  config:
    activate:
      on-profile: "!test"
    import: optional:configserver:${CONFIG_SERVER_URL:http://localhost:8888}
